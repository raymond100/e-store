function Product(id, name, price, origin, quantity, inStock) {
  this.id = id;
  this.name = name;
  this.price = price;
  this.origin = origin;
  this.quantity = quantity;
  this.inStock = inStock;
}

export default Product;
